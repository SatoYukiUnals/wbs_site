# 【結合テスト：データ一覧】（対象：〇〇機能 詳細画面）

> テスト実施前に事前作成しておく前提データを定義する。
> 各テストケースの「操作対象データ」欄では、該当する `Case No.` を参照すること。
> 期待値（更新後の状態等）は `手順書.md` の「期待結果」に記載し、このファイルには含めない。
> このファイルに記載のないカラム・項目は、テストの目的に影響しない範囲で任意の値を設定してよい。

---

## 1. 事前作成データ

### ■ `テナント`

- **Case 1**：テスト用テナント
  - `create_sample_data.py` で作成されたテスト用テナント（対象環境のドメインを使用）

---

### ■ `ユーザー`

- **Case 2**：ログインユーザー（自分）
  - `name` = ユーザーA
  - `department` = 営業部
  - `role` = （対象画面で操作可能なロール）

- **Case 3**：自部署の他ユーザー
  - `name` = ユーザーB
  - `department` = 営業部（Case 2 と同じ部署）
  - `role` = 任意

- **Case 4**：他部署のユーザー
  - `name` = ユーザーC
  - `department` = 経理部（Case 2 と異なる部署）
  - `role` = 任意

- **Case 5〜**：権限テスト用ユーザー（対象画面のロール定義に合わせて追加）
  - `role` = （各ロール名）
  - 用途：6.1.1 ロールによるボタンの表示制御、6.2.1 URLダイレクトアクセス

---

### ■ `対象機能のメインとなるテーブル`

#### 基本表示確認用（手順書 1.1.1 / 1.1.2 / 2.1.1）

- **Case 10**：基本データ（全フィールド入力済み）
  - 全フィールドに値が入っている
  - `status` = 任意
  - `owner` = Case 2（自分）
  - 用途：初期表示確認、フィールド表示形式確認、関連画面への遷移確認

#### 金額・計算項目確認用（手順書 2.2.1 / 2.2.2）

- **Case 11**：関連マスタの各組み合わせパターン分のデータ
  - マスタの組み合わせごとに1件ずつ作成する
  - `status` = 任意
  - `owner` = Case 2（自分）

#### ステータス別表示確認用（手順書 1.2.1 / 2.4.1）

- **Case 13〜**：各ステータス値のデータ（ステータスの種類数分作成する）
  - `status` = （各ステータス値）
  - `owner` = Case 2（自分）
  - 用途：ステータスによるボタン活性制御、フラグ・ラベル表示確認、帳票出力制御

#### ステータス変更用（手順書 4.1.1 / 4.1.2 / 4.2.1）

- **Case 20**：正方向のステータス変更が可能なデータ
  - `status` = （変更前ステータス）
  - `owner` = Case 2（自分）

- **Case 21**：取消・差戻が可能なデータ
  - `status` = （取消・差戻が可能なステータス）
  - `owner` = Case 2（自分）

#### 帳票出力用（手順書 5.1.1）

- **Case 30**：帳票出力が可能なステータスのデータ
  - `status` = （帳票出力可能なステータス）
  - 全フィールドに値が入っている
  - `owner` = Case 2（自分）
