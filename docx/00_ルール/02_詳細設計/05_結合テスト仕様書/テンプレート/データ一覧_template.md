# 【結合テスト：データ一覧】（対象：〇〇機能）

> テスト実施前に事前作成しておく前提データを定義する。
> 各テストケースの「操作対象データ」欄では、該当する `Case No.` を参照すること。
> 期待値（更新後の状態等）は `テスト項目一覧.md` の「期待結果」に記載し、このファイルには含めない。
> このファイルに記載のないカラム・項目は、テストの目的に影響しない範囲で任意の値を設定してよい。
>
> **Case 番号の採番ルール**：テーブル単位で番号帯を区切ること（例：テナント=1〜、ユーザー=2〜、メインテーブル正常系=10〜、異常系=20〜、ステータス各種=30〜）。採番は飛び番でよいが、各テーブルの開始番号をコメントで明示すること。

---

## 1. 事前作成データ

### ■ `テナント`

- **Case 1**：テスト用テナント
  - `create_sample_data.py` で作成されたテスト用テナント（対象環境のドメインを使用）

---

### ■ `ユーザー`

- **Case 2**：ログインユーザー（自分）
  - `name` = ユーザーA
  - `department` = 営業部
  - `role` = （対象画面で操作可能なロール）

- **Case 5〜**：権限テスト用ユーザー（対象画面のロール定義に合わせて追加）
  - `role` = （各ロール名）
  - 用途：ロールによるボタンの表示制御、URLダイレクトアクセス

---

### ■ `対象機能のメインとなるテーブル`

- **Case 10**：正常系用データ
  - `カラム名` = 値
  - 用途：（用途を記載）

- **Case 11**：異常系・境界値用データ
  - `カラム名` = 値
  - 用途：（用途を記載）
