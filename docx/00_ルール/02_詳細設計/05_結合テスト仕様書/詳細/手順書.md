> テスト実施前に [データ一覧.md](データ一覧.md) に記載のデータを作成してから各テストを実施すること。

---

# 1. 初期表示・画面状態

## 1.1. データの初期表示

### ■ 1.1.1. 全フィールドの値が正しく表示されること
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 10

#### ▶ テスト内容
1. 対象データの詳細画面を表示する

#### ▶ 期待結果
- 各フィールドの値がデータベースに保存された値と一致して表示されること
- 空値のフィールドは「-」または空欄で表示されること

---

### ■ 1.1.2. URLダイレクトアクセスによる初期表示
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 10

#### ▶ テスト内容
1. 対象データのIDを含む詳細画面URLに直接アクセスする（例：`/orders/123/`）

#### ▶ 期待結果
- エラー画面が表示されず、対象データの詳細が正常に表示されること

---

### ■ 1.1.3. 存在しないIDへのアクセス
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：（DBに存在しないID）

#### ▶ テスト内容
1. DBに存在しないIDを含む詳細画面URL（例：`/orders/99999/`）に直接アクセスする

#### ▶ 期待結果
- 404 エラーページが表示されること
- アプリがクラッシュしないこと

---

## 1.2. ステータスによる表示制御

### ■ 1.2.1. ステータスに応じたボタン活性制御
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：各ステータスのデータ（Case 13〜）

#### ▶ テスト内容
1. 各ステータスのデータの詳細画面を表示する

#### ▶ 期待結果
| ステータス | 期待する操作ボタンの状態 |
|-----------|----------------------|
| （各ステータスを記載） | （活性 or 非活性を記載） |

---

# 2. 各項目の表示

## 2.1. 基本情報の表示

### ■ 2.1.1. 各フィールドの表示形式
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 10

#### ▶ テスト内容
1. 詳細画面を表示し、各フィールドの表示形式を確認する

#### ▶ 期待結果
| フィールド名 | 期待する表示形式 |
|------------|--------------|
| 日付系フィールド | `YYYY-MM-DD` 形式で表示されること |
| 金額系フィールド | 3桁カンマ区切りで表示されること |
| ステータス | 対応するラベル・色が表示されること |

---

## 2.2. 金額・計算項目の表示

### ■ 2.2.1. 関連マスタの組み合わせによる計算（網羅パターン）
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 11

#### ▶ テスト内容
1. 各マスタ組み合わせパターンのデータの詳細画面を表示する

#### ▶ 期待結果
- 各パターンの計算式に基づいた値が表示されること
- 仕様書に定義した計算結果と一致していること

---

### ■ 2.2.2. 合計値の表示
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 11

#### ▶ テスト内容
1. 明細を複数件持つデータの詳細画面を表示する

#### ▶ 期待結果
- 合計値が各明細の合算値と一致して表示されること
- 合計がマイナスになる場合は「-」付きで表示されること

---

# 3. 画面遷移

## 3.1. 一覧画面への戻り

### ■ 3.1.1. 「戻る」ボタン・パンくずによる一覧への遷移
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 10

#### ▶ テスト内容
1. 一覧画面から詳細画面に遷移する
2. 「戻る」ボタンを押下する
3. 再度詳細画面に遷移してパンくずの「一覧」リンクを押下する

#### ▶ 期待結果
- 手順2・3：一覧画面に遷移すること
- 手順2：一覧画面の検索条件・ページが保持されていること（遷移前の状態が復元されること）

---

## 3.2. 関連画面への遷移

### ■ 3.2.1. 関連データへのリンク押下時の遷移
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 10

#### ▶ テスト内容
1. 関連データへのリンク（例：取引先名リンク）を押下する

#### ▶ 期待結果
- 対応する「関連画面」（画面ID・タイトル）に遷移すること

---

# 4. ステータス変更

## 4.1. 書類ステータス変更

### ■ 4.1.1. 各ステータス遷移（正方向）
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 20

#### ▶ テスト内容
1. 正方向に遷移可能なステータスのデータの詳細画面を表示する
2. 「ステータス変更」ボタンを押下する
3. 「確認ダイアログ」がある場合は「OK」を押下する

#### ▶ 期待結果
- 対象データのステータスが次のステータスに更新されること
- 詳細画面のステータス表示に反映されること
- 変更成功のトーストが表示されること

---

### ■ 4.1.2. 各ステータス遷移（取消・差戻）
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 21

#### ▶ テスト内容
1. 取消・差戻が可能なステータスのデータの詳細画面を表示する
2. 「取消」「差戻」ボタンを押下する
3. 「確認ダイアログ」がある場合は「OK」を押下する

#### ▶ 期待結果
- 対象データのステータスが取消・差戻後のステータスに更新されること
- 詳細画面のステータス表示に反映されること
- 変更成功のトーストが表示されること

---

## 4.2. 表示制御

### ■ 4.2.1. 更新後のステータスによる表示制御
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 20

#### ▶ テスト内容
1. 各ステータスへのステータス変更を実行する

#### ▶ 期待結果
| ステータス | 期待する操作ボタンの状態 |
|-----------|----------------------|
| （各ステータスを記載） | （活性 or 非活性を記載） |

---

# 5. 帳票出力

## 5.1. PDF・Excel出力

### ■ 5.1.1. 帳票出力（正常系）
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 30

#### ▶ テスト内容
1. 「帳票」ボタンを押下する

#### ▶ 期待結果
- 対象形式（PDF 等）のファイルがダウンロードされること
- ファイルの内容が詳細画面に表示されている値と一致していること

---

### ■ 5.1.2. 帳票出力（ステータスによる制御）
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：各ステータスのデータ（Case 13〜）

#### ▶ テスト内容
1. 帳票出力が不可なステータスのデータの詳細画面を表示する

#### ▶ 期待結果
- 「帳票」ボタンが非活性（押下不可）であること

---

# 6. 権限制御

## 6.1. 表示制御

### ■ 6.1.1. ロールによるボタンの表示制御

→ [6.1.1. ロールによるボタンの表示制御.md](6.1.1.%20ロールによるボタンの表示制御.md) を参照

---

## 6.2. URLダイレクトアクセス

### ■ 6.2.1. 権限なし・権限あり・特殊ロール
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 10

#### ▶ テスト内容
1. 権限のないユーザーで詳細画面URLに直接アクセスする
2. 権限のあるユーザーで詳細画面URLに直接アクセスする
3. 特殊ロールのユーザーで詳細画面URLに直接アクセスする

#### ▶ 期待結果
- 手順1：対象画面が表示されず、「ログイン画面」またはエラーページ（403）にリダイレクトされること
- 手順2：エラー画面が表示されず、対象データの詳細が正常に表示されること
- 手順3：特殊ロールに対応した画面が表示され、そのロールに対応したボタンのみ表示されること

---

### ■ 6.2.2. 他テナントのデータへのアクセス
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：（他テナントのデータID）

#### ▶ テスト内容
1. 他テナントのデータIDを含む詳細画面URLに直接アクセスする

#### ▶ 期待結果
- 403 または 404 エラーページが表示されること
- 他テナントのデータが表示されないこと

---

# 7. 非機能・その他

## 7.1. 連打対策

### ■ 7.1.1. 全ボタン・全操作でAPIが1回しか実行されないこと
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 20

#### ▶ テスト内容
1. 各ボタンをすばやく2〜3回連続して押下する

#### ▶ 期待結果
- APIの呼び出しがブラウザの「ネットワーク」タブで1回のみ確認されること
- 1回目の押下後にボタンが非活性になること
- 重複した更新・削除が発生していないこと

---

## 7.2. 操作フィードバック

### ■ 7.2.1. 操作成功後のトースト表示（メッセージ内容・表示タイミング）
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 20

#### ▶ テスト内容
1. 各操作（ステータス変更・削除等）を正常に完了させる

#### ▶ 期待結果
| 操作 | トーストのメッセージ | 表示タイミング | 消えるタイミング |
|-----|-----------------|-------------|--------------|
| ステータス変更 | （仕様書のメッセージを記載） | 変更完了直後 | {N}秒後に自動消去 |
| 削除 | （仕様書のメッセージを記載） | 削除完了直後 | {N}秒後に自動消去 |

---

# 8. ○○ダイアログ（ダイアログがある場合に都度追加）

## 8.1. 中分類

### ■ 8.1.1. 小分類
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1.

#### ▶ 期待結果
-
