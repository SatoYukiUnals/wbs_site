# 【API設計書】プロジェクト別ダッシュボード取得

## 1. 概要

指定プロジェクトの詳細な進捗情報（クォーター別進捗・担当者別タスク数・遅延タスク一覧）を返す。

## 2. エンドポイント

- **Method:** `GET`
- **Path:** `/api/v1/projects/{project_id}/dashboard/`
- **認証:** JWT必須（プロジェクトメンバー以上）

## 3. リクエスト

### パスパラメータ

#### `project_id`（プロジェクトID）

- **型:** `integer` / **必須:** ○

## 4. レスポンス

### 成功 (200 OK)

```json
{
  "project": {
    "id": 1,
    "name": "新規システム開発",
    "progress": 35,
    "start_date": "2026-04-01",
    "end_date": "2026-09-30"
  },
  "quarters": [
    {
      "id": 1,
      "title": "Q1",
      "progress": 60
    }
  ],
  "task_counts": {
    "未着手": 10,
    "進行中": 8,
    "レビュー待ち": 2,
    "完了": 15,
    "保留": 1
  },
  "delayed_tasks": [
    {
      "id": 5,
      "title": "テスト実施",
      "end_date": "2026-04-10",
      "status": "進行中",
      "assignees": [{"user_id": 2, "username": "taro"}]
    }
  ]
}
```

### エラー (4xx / 5xx)

| 状況 | ステータス | エラーコード | メッセージ内容 | 備考 |
| :--- | :--- | :--- | :--- | :--- |
| 未認証 | 401 | `ERR_AUTH_002` | 認証が必要です | |
| 権限不足 | 403 | `ERR_PERM_001` | この操作を行う権限がありません | |
| 対象未存在 | 404 | `ERR_DAT_404` | 対象のデータが見つかりませんでした | |
| サーバーエラー | 500 | `ERR_SYS_500` | システムエラーが発生しました | |

## 5. 内部処理 / ロジック

1. **JWT検証・メンバー確認**
   - `ProjectMember` でメンバーシップを確認
     - 未認証：401 `ERR_AUTH_002` を返す
     - 非メンバー：403 `ERR_PERM_001` を返す
2. **プロジェクト確認**
   - `Project` を `project_id` AND `tenant_id` で SELECT
     - 未存在：404 `ERR_DAT_404` を返す
3. **データ取得・集計**
   - `Quarter` を `project_id` でフィルタして SELECT
   - `Task` をステータス別に集計
   - 遅延タスク（`end_date < 今日` AND `status != 完了`）を SELECT
   - `TaskAssignee` JOIN で担当者情報を取得
     - DBエラー：500 `ERR_SYS_500` を返す
4. **レスポンス生成**
   - プロジェクト詳細・クォーター進捗・タスク集計・遅延タスクを返却
