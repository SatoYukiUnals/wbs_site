# 【結合テスト：データ一覧】（対象：〇〇機能 登録・変更画面）

> テスト実施前に事前作成しておく前提データを定義する。
> 各テストケースの「操作対象データ」欄では、該当する `Case No.` を参照すること。
> 期待値（更新後の状態等）は `02_手順書.md` の「期待結果」に記載し、このファイルには含めない。
> このファイルに記載のないカラム・項目は、テストの目的に影響しない範囲で任意の値を設定してよい。

---

## 1. 事前作成データ

### ■ `テナント`

- **Case 1**：テスト用テナント
  - `create_sample_data.py` で作成されたテスト用テナント（対象環境のドメインを使用）

---

### ■ `ユーザー`

- **Case 2**：ログインユーザー（自分）
  - `name` = ユーザーA
  - `department` = 営業部
  - `role` = （対象画面で操作可能なロール）

- **Case 5〜**：権限テスト用ユーザー（対象画面のロール定義に合わせて追加）
  - `role` = （各ロール名）
  - 用途：ロールによるボタンの表示制御、URLダイレクトアクセス

---

### ■ `customers`（前提マスタ）

- **Case 10**：正常登録・変更操作用の顧客
  - `id` = 101
  - `name` = テスト顧客A
  - `email` = `test_a@example.com`
  - 用途：正常系

- **Case 11**：権限外データ確認用の顧客
  - `id` = 102
  - `name` = テスト顧客B
  - `email` = `test_b@example.com`
  - 用途：他者データへのアクセス確認

---

### ■ `orders`

- **Case 20**：正常登録ケース用
  - `id` = 201
  - `order_no` = `ORD-2024-001`
  - `customer_id` = 101（Case 10）
  - `amount` = 5000
  - `status` = `ordered`
  - 用途：正常登録の確認

- **Case 21**：重複チェック用
  - `id` = 202
  - `order_no` = `ORD-2024-001`（Case 20 と同じ注文番号）
  - `customer_id` = 101（Case 10）
  - `amount` = 3000
  - `status` = `ordered`
  - 用途：一意制約による重複エラーの確認

- **Case 22**：正常更新対象データ
  - `id` = 203
  - `order_no` = `ORD-2024-002`
  - `customer_id` = 101（Case 10）
  - `amount` = 2000
  - `status` = `ordered`
  - 用途：正常更新・ステータス遷移の確認

- **Case 23**：遷移不可ステータスのデータ
  - `id` = 204
  - `order_no` = `ORD-2024-003`
  - `customer_id` = 101（Case 10）
  - `amount` = 1500
  - `status` = `completed`
  - 用途：遷移禁則エラーの確認