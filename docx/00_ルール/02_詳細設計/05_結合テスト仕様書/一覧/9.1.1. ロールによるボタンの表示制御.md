# 9.1.1. ロールによるボタンの表示制御

> 各ロールでログインしたときに表示・非表示となるボタンを確認するテストケース。
> 実際の仕様書作成時は、ロール名・ボタン名を対象画面の権限定義に合わせて置き換えること。

---

#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 各ロールのユーザーでログインし画面を表示する

#### ▶ 期待結果

**ロール：（ロール名）**
- 「登録」ボタンが（表示 / 非表示）であること
- 「変更」ボタンが（表示 / 非表示）であること
- 「複製」ボタンが（表示 / 非表示）であること
- 「更新」ボタンが（表示 / 非表示）であること
- 「終了」ボタンが（表示 / 非表示）であること
- 「再開」ボタンが（表示 / 非表示）であること
- 「帳票」ボタンが（表示 / 非表示）であること
- 「一括削除」ボタンが（表示 / 非表示）であること
- 「一括ステータス更新」ボタンが（表示 / 非表示）であること
- 「一括登録」ボタンが（表示 / 非表示）であること

---

**ロール：（ロール名）**
- 「登録」ボタンが（表示 / 非表示）であること
- 「変更」ボタンが（表示 / 非表示）であること
- 「複製」ボタンが（表示 / 非表示）であること
- 「更新」ボタンが（表示 / 非表示）であること
- 「終了」ボタンが（表示 / 非表示）であること
- 「再開」ボタンが（表示 / 非表示）であること
- 「帳票」ボタンが（表示 / 非表示）であること
- 「一括削除」ボタンが（表示 / 非表示）であること
- 「一括ステータス更新」ボタンが（表示 / 非表示）であること
- 「一括登録」ボタンが（表示 / 非表示）であること

---

（ロール数に合わせて上記ブロックを追加する）
