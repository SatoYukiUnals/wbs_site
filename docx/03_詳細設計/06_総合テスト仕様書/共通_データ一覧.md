# 【総合テスト：データ一覧】（全シナリオ共通）

> テスト実施前に事前作成しておく前提データを定義する。
> 各テストケースの「前提条件」欄では、該当する `Case No.` を参照すること。
> 期待値（更新後の状態等）は各テスト項目一覧の「期待結果」に記載する。

---

## 1. 事前作成データ

### ■ `テナント`（Case 1〜）

- **Case 1**：総合テスト用テナント
  - `name` = 総合テストテナント

---

### ■ `ユーザー`（Case 2〜）

- **Case 2**：adminユーザー（主にレビュアー・管理操作役）
  - `email` = `st_admin@example.com`
  - `password` = `Test1234!`
  - `display_name` = 管理者ユーザー
  - `role` = `admin`
  - `tenant` = Case 1

- **Case 3**：memberユーザーA（主にタスク担当者役）
  - `email` = `st_member@example.com`
  - `password` = `Test1234!`
  - `display_name` = メンバーユーザーA
  - `role` = `member`
  - `tenant` = Case 1

---

### ■ `プロジェクト`（Case 5〜）

- **Case 5**：基本テスト用プロジェクト（シナリオ01・02・03で使用）
  - `name` = 総合テストプロジェクトA
  - `start_date` = `2026-04-01`
  - `end_date` = `2026-12-31`
  - メンバー：Case 2（admin）、Case 3（member）

---

### ■ `クォーター`（Case 8〜）

- **Case 8**：Q1（シナリオ01〜03で使用）
  - `title` = Q1、`start_date` = `2026-04-01`、`end_date` = `2026-06-30`
  - `project` = Case 5

- **Case 9**：Q2（シナリオ01で使用）
  - `title` = Q2、`start_date` = `2026-07-01`、`end_date` = `2026-09-30`
  - `project` = Case 5

---

### ■ `タスク`（Case 10〜・シナリオ02・03で使用）

- **Case 10**：タスクA（進捗管理フローのメインタスク）
  - `title` = タスクA
  - `status` = 未着手、`progress` = 0
  - `priority` = 高
  - `quarter` = Case 8
  - 担当者：Case 3（memberユーザー）
  - `project` = Case 5

- **Case 20**：差し戻しフロー用タスク（レビュー待ち）
  - `title` = 差し戻しテスト用タスク
  - `status` = レビュー待ち
  - `progress` = 100
  - 担当者：Case 3
  - `project` = Case 5
  - ※ reviewレコードも事前作成（`status` = pending）

---

### ■ `プロジェクト`（Case 30〜・シナリオ04で使用）

- **Case 30**：報告書・Excel出力テスト用プロジェクト
  - `name` = 出力テストプロジェクト
  - `start_date` = `2026-01-01`、`end_date` = `2026-06-30`
  - メンバー：Case 2（admin）

- **Case 31**：Q1（Case 30 に紐づく）
  - `title` = Q1、`start_date` = `2026-01-01`、`end_date` = `2026-03-31`
  - `project` = Case 30

### ■ `タスク`（Case 32〜・シナリオ04で使用）

- **Case 32**：完了タスク
  - `title` = 完了タスク
  - `status` = 完了、`progress` = 100
  - `start_date` = `2026-01-01`、`end_date` = `2026-01-31`
  - `actual_start_date` = `2026-01-03`、`actual_end_date` = `2026-01-28`
  - `quarter` = Case 31

- **Case 33**：進行中タスク（actual未入力・StatusHistoryで補完）
  - `title` = 進行中タスク
  - `status` = 進行中、`progress` = 60
  - `start_date` = `2026-02-01`、`end_date` = `2026-02-28`
  - `actual_start_date` = NULL、`actual_end_date` = NULL
  - StatusHistory：`status=進行中` の `changed_at` = `2026-02-05`
  - `quarter` = Case 31

- **Case 34**：遅延タスク（終了日超過・未完了）
  - `title` = 遅延タスク
  - `status` = 進行中、`progress` = 30
  - `start_date` = `2026-01-15`、`end_date` = `2026-01-31`（過去日）
  - `quarter` = Case 31
