# 【結合テスト：データ一覧】（対象：進捗・ステータス管理機能）

---

## 1. 事前作成データ

### ■ `テナント`（Case 1〜）

- **Case 1**：テスト用テナント

### ■ `ユーザー`（Case 2〜）

- **Case 2**：adminユーザー（`role` = `admin`）
- **Case 3**：memberユーザー（`role` = `member`）

### ■ `プロジェクト`（Case 10〜）

- **Case 10**：進捗テスト用プロジェクト
  - `progress` = 0（初期）
  - メンバー：Case 2（admin）、Case 3（member）

### ■ `クォーター`（Case 8〜）

- **Case 8**：Q1（タスクCase 11, 12 に紐づく）

### ■ `タスク`（Case 11〜）

- **Case 11**：進捗更新テスト用タスク（担当者：Case 3）
  - `title` = 進捗テストタスクA
  - `status` = 未着手
  - `progress` = 0
  - `quarter` = Case 8
  - 担当者：Case 3

- **Case 12**：完了タスク（担当者：Case 2）
  - `title` = 進捗テストタスクB
  - `status` = 完了
  - `progress` = 100
  - `quarter` = Case 8
  - 担当者：Case 2

- **Case 13**：遅延タスク（終了日超過・未完了）
  - `title` = 遅延テストタスク
  - `status` = 進行中
  - `progress` = 20
  - `end_date` = `2026-01-01`（過去日）
  - `project` = Case 10
