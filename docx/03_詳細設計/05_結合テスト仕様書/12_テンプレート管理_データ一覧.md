# 【結合テスト：データ一覧】（対象：テンプレート管理機能）

---

## 1. 事前作成データ

### ■ `テナント`（Case 1〜）

- **Case 1**：テスト用テナント

### ■ `ユーザー`（Case 2〜）

- **Case 2**：adminユーザー（`role` = `admin`）
- **Case 3**：memberユーザーA（`role` = `member`）
- **Case 4**：memberユーザーB（`role` = `member`、他ユーザーの個人テンプレート作成用）

### ■ `プロジェクト`（Case 5〜）

- **Case 5**：テンプレートテスト用プロジェクト
  - メンバー：Case 2（admin）、Case 3（member）、Case 4（member）

### ■ `テンプレート`（Case 10〜）

- **Case 10**：共有WBSテンプレート
  - `title` = 共有WBSテンプレートA
  - `type` = wbs
  - `content` = "タスクA\nタスクB\nタスクC"（3行）
  - `is_shared` = True
  - `created_by` = Case 2

- **Case 11**：Case 3 の個人テンプレート（削除テスト用）
  - `title` = Case3の個人テンプレート
  - `type` = task
  - `content` = "テスト内容"
  - `is_shared` = False
  - `created_by` = Case 3

- **Case 12**：Case 4 の個人テンプレート（Case 3 から見えないこと確認用）
  - `title` = Case4の個人テンプレート
  - `type` = wbs
  - `is_shared` = False
  - `created_by` = Case 4

### ■ `タスク`（Case 13〜）

- **Case 13**：テンプレート適用テスト用タスク（階層1）
  - `title` = テンプレート適用テストタスク
  - `project` = Case 5
  - 階層1（親タスクなし）

- **Case 16**：5層目タスク（階層上限テスト用）
  - 階層1〜5 の連続した親子タスクを作成する
  - 最下層（5層目）のタスク
  - `project` = Case 5
