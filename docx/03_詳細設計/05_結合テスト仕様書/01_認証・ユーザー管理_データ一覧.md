# 【結合テスト：データ一覧】（対象：認証・ユーザー管理機能）

> テスト実施前に事前作成しておく前提データを定義する。
> 各テストケースの「操作対象データ」欄では、該当する `Case No.` を参照すること。
> 期待値は `テスト項目一覧.md` の「期待結果」に記載する。

---

## 1. 事前作成データ

### ■ `テナント`（Case 1〜）

- **Case 1**：テスト用テナント
  - `name` = テストテナント（結合テスト用）

---

### ■ `ユーザー`（Case 2〜）

- **Case 2**：masterユーザー（テスト用）
  - `email` = `master@example.com`
  - `password` = `Test1234!`
  - `display_name` = マスターユーザー
  - `role` = `master`
  - `tenant` = Case 1

- **Case 3**：memberユーザー（テスト用）
  - `email` = `member@example.com`
  - `password` = `Test1234!`
  - `display_name` = メンバーユーザー
  - `role` = `member`
  - `tenant` = Case 1

- **Case 4**：adminユーザー（テスト用）
  - `email` = `admin@example.com`
  - `password` = `Test1234!`
  - `display_name` = 管理者ユーザー
  - `role` = `admin`
  - `tenant` = Case 1
