> テスト実施前に [データ一覧.md](データ一覧.md) に記載のデータを作成してから各テストを実施すること。

---

# 1. 初期表示・画面状態

## 1.1. 検索欄の初期状態

### ■ 1.1.1. 検索欄・ステータスの初期値
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. パラメータなしで画面URLに直接アクセスする

#### ▶ 期待結果
- 「開始日」・「終了日」がともに空欄で表示されること
- 「キーワード検索」欄が空欄で表示されること
- 「ステータス」セレクトが「すべて」で表示されること

---

## 1.2. URLパラメータによる初期設定

### ■ 1.2.1. クエリパラメータによる検索条件の自動設定
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 検索条件を指定したクエリパラメータ付きURL（例：`?status=active&keyword=テスト`）でアクセスする

#### ▶ 期待結果
- URLパラメータで指定した値が各検索欄にセットされた状態で表示されること
- その条件で絞り込まれた一覧が表示されること

---

## 1.3. 表示列の選択

### ■ 1.3.1. 列ごとのON/OFF切り替え
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 「表示列の選択」で任意の列（例：「件名」）をOFFにする
2. 同じ列をONに戻す

#### ▶ 期待結果
- 手順1：OFFにした列が一覧のヘッダー・データ行から非表示になること
- 手順2：OFFにした列が一覧のヘッダー・データ行に再表示されること

---

## 1.4. 状態の永続化

### ■ 1.4.1. LocalStorage / SessionStorageへの保存と復元
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 「キーワード検索」欄に以下を入力する
   ```
   テスト
   ```
2. 「開始日」に以下を入力して「検索」ボタンを押下する
   ```
   2026-04-01
   ```
3. ソート列を「件名」列に変更する
4. 表示件数を「50件」に変更する
5. ブラウザの開発ツール（F12 → Application → Local Storage / Session Storage）でStorageの内容を確認する
6. ページをリロードする
7. 別画面に移動後、当該画面に戻る

#### ▶ 期待結果
- 手順5：検索条件・ソート条件・表示件数がLocalStorage / SessionStorageに保存されていること
- 手順6・7：変更した検索条件・ソート条件・表示件数が復元された状態で表示されること

---

## 1.5. 検索条件のリセット

### ■ 1.5.1. リセットボタンによる検索条件の解除
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 「キーワード検索」欄に以下を入力する
   ```
   テスト
   ```
2. 「開始日」に以下を入力する
   ```
   2026-04-01
   ```
3. 「ステータス」を任意のステータスに変更する
4. 「リセット」ボタンを押下する

#### ▶ 期待結果
- 「キーワード検索」欄が空欄になること
- 「日付・期間」欄が空欄になること
- 「ステータス」セレクトが「すべて」に戻ること
- 全件を対象とした一覧が表示されること

---

## 1.6. 表示制御

### ■ 1.6.1. 初期状態によるボタン活性制御
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. チェックボックスを1件も選択しない状態で画面を表示する

#### ▶ 期待結果
- 「一括操作」ボタンが非活性（押下不可）で表示されること

---

# 2. 検索・絞り込み

## 2.1. 単一条件

### ■ 2.1.1. キーワード検索（各検索フィールド）
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 「キーワード検索」欄に一覧に存在するデータの値の一部（例：下記）を入力する
   ```
   テスト受注
   ```
2. 「検索」ボタンを押下する

#### ▶ 期待結果
- 入力したキーワードを含むデータのみ表示されること
- キーワードを含まないデータは1件も表示されないこと
- 件数表示が絞り込み後の件数になっていること

---

### ■ 2.1.2. 期間検索（期間内・開始日・終了日）
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 「開始日」・「終了日」に以下をそれぞれ入力して検索する
   ```
   開始日：2026-04-01
   終了日：2026-04-21
   ```
2. 「開始日」に以下のみ入力して検索する
   ```
   2026-04-01
   ```
3. 「終了日」に以下のみ入力して検索する
   ```
   2026-04-21
   ```

#### ▶ 期待結果
- 手順1：「開始日」〜「終了日」の範囲内のデータのみ表示されること
- 手順2：「開始日」以降のデータのみ表示されること
- 手順3：「終了日」以前のデータのみ表示されること
- いずれのパターンも範囲外のデータは1件も表示されないこと

---

### ■ 2.1.3. ステータス絞り込み
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 各ステータスを選択して検索する

#### ▶ 期待結果
- 選択したステータスのデータのみ表示されること
- 他のステータスのデータは1件も表示されないこと

---

### ■ 2.1.4. 所有者絞り込み（すべて・自部署・自分）
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 所有者絞り込みを「すべて」に変更する
2. 所有者絞り込みを「自部署」に変更する
3. 所有者絞り込みを「自分」に変更する

#### ▶ 期待結果
- 手順1：全所有者のデータが表示されること
- 手順2：ログインユーザーの部署が所有者のデータのみ表示されること
- 手順3：ログインユーザー本人が所有者のデータのみ表示されること

---

## 2.2. 複合条件

### ■ 2.2.1. 上記の組み合わせ（網羅表に基づく全パターン）
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 複数の検索条件を同時に設定して検索する（網羅表の各パターン）

#### ▶ 期待結果
- 設定した全条件を同時に満たすデータのみ表示されること
- いずれかの条件を満たさないデータは1件も表示されないこと

---

## 2.3. 実行方法

### ■ 2.3.1. ボタン押下・Enterキーによる検索実行
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 「キーワード検索」欄に以下を入力し、「検索」ボタンを押下する
   ```
   テスト
   ```
2. 「キーワード検索」欄に以下を入力し、Enterキーを押す
   ```
   テスト
   ```

#### ▶ 期待結果
- いずれの操作でもAPIが1回実行されること
- 入力した条件に一致するデータに一覧が更新されること

---

## 2.4. バリデーション

→ [2.4. バリデーション.md](2.4.%20バリデーション.md) を参照

---

# 3. ソート

## 3.1. 基本動作

### ■ 3.1.1. 各列の昇順・降順
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. ソート対象の列ヘッダー（例：「件名」）を1回押下する
2. 同じ列ヘッダーをもう1回押下する

#### ▶ 期待結果
- 手順1：押下した列ヘッダーに昇順アイコンが表示され、その列の値で昇順に並んでいること
- 手順1：他の列にソートアイコンが表示されていないこと
- 手順2：押下した列ヘッダーに降順アイコンが表示され、その列の値で降順に並んでいること

---

### ■ 3.1.2. 初期ソート状態の確認
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. パラメータなしで画面を初期表示する

#### ▶ 期待結果
- 仕様書に定義した初期ソート列にアイコンが表示されること
- 定義した方向（昇順 or 降順）でデータが並んでいること

---

## 3.2. 他機能との連携

### ■ 3.2.1. 期間絞り込みあり・なしでの動作
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 期間を指定せず全件表示した状態でソートを実行する
2. 「開始日」・「終了日」に以下をそれぞれ設定して検索した状態でソートを実行する
   ```
   開始日：2026-04-01
   終了日：2026-04-21
   ```

#### ▶ 期待結果
- 手順1：絞り込み前と件数が変わらず、指定列・方向でソートされていること
- 手順2：期間絞り込みの結果件数が変わらず、指定列・方向でソートされていること

---

# 4. ページ操作

## 4.1. ページ送り

### ■ 4.1.1. 次ページ・前ページ
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 1ページ目を表示した状態で「次ページ」ボタンを押下する
2. 2ページ目を表示した状態で「前ページ」ボタンを押下する

#### ▶ 期待結果
- 手順1：ページ番号が1増え、次のページのデータに切り替わること
- 手順2：ページ番号が1減り、前のページのデータに切り替わること

---

### ■ 4.1.2. 最終ページへの移動
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 「次ページ」ボタンを複数回押下して最終ページまで移動する

#### ▶ 期待結果
- 最終ページに到達すると「次ページ」ボタンが非活性（押下不可）になること
- 最終ページのデータが表示されること

---

## 4.2. ページリセット

### ■ 4.2.1. 検索条件・ソート変更時のページリセット
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 2ページ目以降に移動する
2. 検索条件を変更して「検索」ボタンを押下する
3. 再度2ページ目以降に移動する
4. ソートを変更する

#### ▶ 期待結果
- 手順2・4ともにページ番号が1に戻ること
- 変更後の条件による1ページ目のデータが表示されること

---

# 5. 表示内容・計算

## 5.1. 数値計算

### ■ 5.1.1. 関連マスタの組み合わせによる計算（網羅パターン）
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 各マスタ組み合わせパターンのデータを一覧表示する（網羅表の各パターン）

#### ▶ 期待結果
- 各パターンの計算式に基づいた値が表示されること
- 網羅表の期待値と一致していること

---

### ■ 5.1.2. 派生項目の計算
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 派生項目を持つデータを一覧表示する

#### ▶ 期待結果
- 派生項目が元データから導出した計算値と一致して表示されること

---

## 5.2. 合計値

### ■ 5.2.1. 正常系
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. プラス値のみのデータを複数件一覧表示する

#### ▶ 期待結果
- 合計値が表示中の全データの合算値と一致していること

---

### ■ 5.2.2. 除外ステータスを含む場合
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 合計値の計算から除外されるステータスのデータを含む一覧を表示する

#### ▶ 期待結果
- 合計値が除外ステータスのデータを含まない合算値と一致していること
- 除外ステータスのデータが合計値に計上されていないこと

---

### ■ 5.2.3. マイナス値を含む場合
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. マイナス値のデータを含む一覧を表示する

#### ▶ 期待結果
- プラス・マイナスを含めた合算値と一致した合計値が表示されること
- 合計がマイナスになる場合は「-」付きで表示されること

---

## 5.3. フラグ表示

### ■ 5.3.1. 各ステータスに対応するフラグの表示
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 全ステータスのデータを一覧表示する

#### ▶ 期待結果
| ステータス | 期待するフラグ・表示 |
|-----------|-------------------|
| （各ステータスを記載） | （対応するラベル・色・アイコンを記載） |

---

## 5.4. 表示制御

### ■ 5.4.1. アラート種別による表示制御
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 全アラート種別のデータを一覧表示する

#### ▶ 期待結果
| アラート種別 | 期待する表示（色・アイコン等） |
|-----------|--------------------------|
| （各アラート種別を記載） | （対応する表示仕様を記載） |

---

## 5.5. アラート絞り込み

### ■ 5.5.1. 各絞り込み条件の組み合わせ
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 各絞り込み条件を単独で設定して検索する
2. 複数の絞り込み条件を組み合わせて検索する

#### ▶ 期待結果
- 設定した絞り込み条件に該当するアラートのデータのみ表示されること
- 条件外のデータは1件も表示されないこと

---

# 6. 画面遷移

## 6.1. 対象データの操作

### ■ 6.1.1. 登録・変更・複製・詳細
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 「登録」ボタンを押下する
2. 変更対象データの「変更」ボタンを押下する
3. 複製対象データの「複製」ボタンを押下する
4. 詳細対象データの「詳細」ボタンを押下する

#### ▶ 期待結果
- 手順1：「登録画面」に遷移すること
- 手順2：「変更画面」に遷移し、対象データの値が各フィールドに入っていること
- 手順3：「登録画面」に遷移し、複製元データの値が各フィールドに入っていること
- 手順4：「詳細画面」に遷移し、対象データの値が各フィールドに表示されていること

---

## 6.2. 関連画面への遷移

### ■ 6.2.1. 遷移（絞り込みなし）
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 絞り込みなしの状態で関連画面へのリンク・ボタンを押下する

#### ▶ 期待結果
- 対応する「関連画面」（画面ID・タイトル）に遷移すること

---

### ■ 6.2.2. 遷移（絞り込みあり）
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 絞り込み条件を設定して検索する
2. 関連画面へのリンク・ボタンを押下する

#### ▶ 期待結果
- 対応する「関連画面」に遷移すること
- 元画面の絞り込み条件が引き継がれていること

---

# 7. ステータス変更

## 7.1. 書類ステータス変更

### ■ 7.1.1. 各ステータス遷移（正方向）
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 正方向に遷移可能なステータスのデータに対し「ステータス変更」ボタンを押下する
2. 「確認ダイアログ」がある場合は「OK」を押下する

#### ▶ 期待結果
- 対象データのステータスが次のステータスに更新されること
- 一覧の表示に反映されること
- 変更成功のトーストが表示されること

---

### ■ 7.1.2. 各ステータス遷移（取消・差戻）
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 取消・差戻が可能なステータスのデータに対し「取消」「差戻」ボタンを押下する
2. 「確認ダイアログ」がある場合は「OK」を押下する

#### ▶ 期待結果
- 対象データのステータスが取消・差戻後のステータスに更新されること
- 変更成功のトーストが表示されること

---

## 7.2. 表示制御

### ■ 7.2.1. 更新後のステータスによる表示制御
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 各ステータスへのステータス変更を実行する

#### ▶ 期待結果
| ステータス | 期待する操作ボタンの状態 |
|-----------|----------------------|
| （各ステータスを記載） | （活性 or 非活性を記載） |

---

# 8. 一括操作

## 8.1. チェックボックス操作

### ■ 8.1.1. 全選択・全解除
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 「全選択」チェックボックスを押下する
2. 「全選択」チェックボックスを再度押下する

#### ▶ 期待結果
- 手順1：「全選択」チェックボックスがチェック状態になり、表示中の全行がチェック状態になること
- 手順1：「選択件数」の表示が表示中の件数と一致し、「一括操作」ボタンが活性（押下可能）になること
- 手順2：「全選択」チェックボックスが未チェック状態になり、全行が未チェック状態になること
- 手順2：「選択件数」が0件になり、「一括操作」ボタンが非活性（押下不可）になること

---

## 8.2. 一括削除

### ■ 8.2.1. 削除可能な条件
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 削除可能なデータを選択する
2. 「一括削除」ボタンを押下する
3. 「確認ダイアログ」の「OK」を押下する

#### ▶ 期待結果
- 選択したデータが一覧から消えること
- 件数が削除した件数分減っていること
- 削除成功のトーストが表示されること

---

### ■ 8.2.2. 削除不可な条件
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 削除不可ステータスのデータを選択する
2. 「一括削除」ボタンを押下する

#### ▶ 期待結果
- 削除不可の旨のエラーメッセージが表示されること
- 対象データが削除されず一覧に残っていること

---

## 8.3. 一括ステータス更新

### ■ 8.3.1. 一括ステータス更新
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 複数データを選択する
2. 「一括ステータス更新」ボタンを押下する
3. 「確認ダイアログ」の「OK」を押下する

#### ▶ 期待結果
- 選択した全データのステータスが更新後ステータスに変わっていること
- 更新成功のトーストが表示されること

---

## 8.4. 一括登録

### ■ 8.4.1. 件数1〜上限・上限+1
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 1件選択して「一括登録」を実行する
2. 上限件数（{上限件数}件）を選択して「一括登録」を実行する
3. 上限件数+1件（{上限件数+1}件）を選択して「一括登録」を実行する

#### ▶ 期待結果
- 手順1・2：エラーメッセージが表示されず、選択件数分のデータが登録されること
- 手順1・2：登録成功のトーストが表示されること
- 手順3：上限件数を超えている旨のエラーメッセージが表示されること
- 手順3：1件も登録されないこと

---

## 8.5. 表示制御

### ■ 8.5.1. 選択件数・状態による一括ボタンの活性制御
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. チェックボックスを1件選択する
2. チェックボックスをすべて解除する
3. 「一括登録」不可なステータスのデータを選択する
4. 「一括登録」可能なステータスのデータを選択する

#### ▶ 期待結果
- 手順1：「一括操作」ボタンが活性（押下可能）になること
- 手順2：「一括操作」ボタンが非活性（押下不可）になること
- 手順3：「一括登録」ボタンが非活性（押下不可）になること
- 手順4：「一括登録」ボタンが活性（押下可能）になること

---

# 9. 権限制御

## 9.1. 表示制御

### ■ 9.1.1. ロールによるボタンの表示制御

→ [9.1.1. ロールによるボタンの表示制御.md](9.1.1.%20ロールによるボタンの表示制御.md) を参照

---

## 9.2. 一括操作権限

### ■ 9.2.1. 一括削除・一括ステータス更新・一括登録
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 権限のないロールのユーザーでログインし、一括操作を実行する

#### ▶ 期待結果
- 操作が実行されないこと（APIが呼び出されない）
- 権限がない旨のエラーメッセージが表示されること
- データの状態が変更されていないこと

---

## 9.3. URLダイレクトアクセス

### ■ 9.3.1. 権限なし・権限あり・特殊ロール
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 権限のないユーザーで画面URLに直接アクセスする
2. 権限のあるユーザーで画面URLに直接アクセスする
3. 特殊ロールのユーザーで画面URLに直接アクセスする

#### ▶ 期待結果
- 手順1：対象画面が表示されず、「ログイン画面」またはエラーページ（403）にリダイレクトされること
- 手順2：エラー画面が表示されず、対象画面が正常に表示されること
- 手順3：特殊ロールに対応した画面が表示され、そのロールに対応したボタンのみ表示されること

---

# 10. 非機能・その他

## 10.1. 連打対策

### ■ 10.1.1. 全ボタン・全操作でAPIが1回しか実行されないこと
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 各ボタンをすばやく2〜3回連続して押下する

#### ▶ 期待結果
- APIの呼び出しがブラウザの「ネットワーク」タブで1回のみ確認されること
- 1回目の押下後にボタンが非活性になること
- 重複登録・更新・削除が発生していないこと

---

## 10.2. データ出力

### ■ 10.2.1. CSV・Excel出力（条件別パターン）
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 絞り込みなしで「CSV出力」ボタンを押下する
2. 絞り込みありで「CSV出力」ボタンを押下する
3. 検索結果0件の状態で「CSV出力」ボタンの状態を確認する
4. 絞り込みなしで「Excel出力」ボタンを押下する
5. 絞り込みありで「Excel出力」ボタンを押下する
6. 検索結果0件の状態で「Excel出力」ボタンの状態を確認する

#### ▶ 期待結果
- 手順1：`.csv` 形式でダウンロードされ、全件のデータとヘッダー行が仕様書通りに含まれていること
- 手順2：絞り込み条件に一致したデータのみ含まれていること
- 手順3：「CSV出力」ボタンが非活性（押下不可）になっていること
- 手順4：`.xlsx` 形式でダウンロードされ、全件のデータとヘッダー行が仕様書通りに含まれていること
- 手順5：絞り込み条件に一致したデータのみ含まれていること
- 手順6：「Excel出力」ボタンが非活性（押下不可）になっていること

---

## 10.3. 操作フィードバック

### ■ 10.3.1. 操作成功後のトースト表示（メッセージ内容・表示タイミング）
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 各操作（登録・変更・削除等）を正常に完了させる

#### ▶ 期待結果
| 操作 | トーストのメッセージ | 表示タイミング | 消えるタイミング |
|-----|-----------------|-------------|--------------|
| 登録 | （仕様書のメッセージを記載） | 登録完了直後 | {N}秒後に自動消去 |
| 変更 | （仕様書のメッセージを記載） | 変更完了直後 | {N}秒後に自動消去 |
| 削除 | （仕様書のメッセージを記載） | 削除完了直後 | {N}秒後に自動消去 |

---

## 10.4. 表示制御

### ■ 10.4.1. データ件数によるデータ出力ボタンの活性制御
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1. 検索結果が0件になる条件で検索する
2. 検索結果が1件以上になる条件で検索する

#### ▶ 期待結果
- 手順1：「データ出力」ボタンが非活性（押下不可）になること
- 手順2：「データ出力」ボタンが活性（押下可能）になること

---

# 11. ○○ダイアログ（ダイアログがある場合に都度追加）

## 11.1. 中分類

### ■ 11.1.1. 小分類
#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：

#### ▶ テスト内容
1.

#### ▶ 期待結果
-
