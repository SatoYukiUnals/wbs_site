# 【結合テスト：データ一覧】（対象：〇〇機能 一覧画面）

> テスト実施前に事前作成しておく前提データを定義する。
> 各テストケースの「操作対象データ」欄では、該当する `Case No.` を参照すること。
> 期待値（更新後の状態等）は `手順書.md` の「期待結果」に記載し、このファイルには含めない。
> このファイルに記載のないカラム・項目は、テストの目的に影響しない範囲で任意の値を設定してよい。

---

## 1. 事前作成データ

### ■ `テナント`

- **Case 1**：テスト用テナント
  - `create_sample_data.py` で作成されたテスト用テナント（対象環境のドメインを使用）

---

### ■ `ユーザー`

- **Case 2**：ログインユーザー（自分）
  - `name` = ユーザーA
  - `department` = 営業部
  - `role` = （対象画面で操作可能なロール）

- **Case 3**：自部署の他ユーザー
  - `name` = ユーザーB
  - `department` = 営業部（Case 2 と同じ部署）
  - `role` = 任意

- **Case 4**：他部署のユーザー
  - `name` = ユーザーC
  - `department` = 経理部（Case 2 と異なる部署）
  - `role` = 任意

- **Case 5〜**：権限テスト用ユーザー（対象画面のロール定義に合わせて追加）
  - `role` = （各ロール名）
  - 用途：9.1.1 ロールによるボタンの表示制御、9.2.1 一括操作権限、9.3.1 URLダイレクトアクセス

---

### ■ `対象機能のメインとなるテーブル`

#### キーワード検索用（手順書 2.1.1 / 2.2.1）

- **Case 10**：キーワード一致データ（自分所有・期間内・受注済み）
  - キーワード対象フィールド = 「テスト受注」を含む値
  - 日付フィールド = 2026-04-15
  - `status` = 受注済み
  - `owner` = Case 2（自分）
  - `owner_department` = 営業部

- **Case 11**：キーワード不一致データ（絞り込み0件確認用）
  - キーワード対象フィールド = 「テスト受注」を含まない値
  - 日付フィールド = 2026-04-15
  - `status` = 受注済み
  - `owner` = Case 2（自分）

#### 期間検索用（手順書 2.1.2 / 3.2.1）

- **Case 12**：期間内データ（2026-04-01〜2026-04-21 の範囲内）
  - 日付フィールド = 2026-04-15
  - `status` = 任意
  - `owner` = Case 2（自分）

- **Case 13**：期間外データ（2026-04-01 より前）
  - 日付フィールド = 2025-12-01
  - `status` = 任意
  - `owner` = Case 2（自分）

- **Case 14**：期間外データ（2026-04-21 より後）
  - 日付フィールド = 2026-07-01
  - `status` = 任意
  - `owner` = Case 2（自分）

#### ステータス絞り込み用（手順書 2.1.3）

- **Case 15〜**：各ステータス値のデータ（ステータスの種類数分作成する）
  - `status` = （各ステータス値）
  - `owner` = Case 2（自分）
  - 用途：対象ステータスを選択したとき、そのデータのみ表示されることを確認

#### 所有者絞り込み用（手順書 2.1.4）

- **Case 20**：自分所有データ
  - `owner` = Case 2（自分）
  - `owner_department` = 営業部

- **Case 21**：自部署・他者所有データ
  - `owner` = Case 3（自部署の他ユーザー）
  - `owner_department` = 営業部

- **Case 22**：他部署所有データ
  - `owner` = Case 4（他部署のユーザー）
  - `owner_department` = 経理部

#### 複合条件検索用（手順書 2.2.1）

- **Case 23**：複合条件パターンA（受注書001相当）
  - キーワード対象フィールド = 「受注書001」を含む値
  - 日付フィールド = 2024-04-15
  - `status` = 受注済み
  - `owner` = Case 2（自分）
  - `owner_department` = 営業部

- **Case 24**：複合条件パターンB（受注書002相当）
  - キーワード対象フィールド = 「受注書002」を含む値
  - 日付フィールド = 2024-07-01
  - `status` = 下書き
  - `owner` = Case 3（自部署・他者）
  - `owner_department` = 営業部

- **Case 25**：複合条件パターンC（受注書003相当）
  - キーワード対象フィールド = 「受注書003」を含む値
  - 日付フィールド = 2023-12-01
  - `status` = 受注済み
  - `owner` = Case 4（他部署・他者）
  - `owner_department` = 経理部

#### ページング用（手順書 4.1.1 / 4.1.2 / 4.2.1）

- **Case 30〜**：1ページの表示件数を超える件数のデータ（デフォルト表示件数 + 1件以上）
  - `owner` = Case 2（自分）または権限範囲内のユーザー
  - 用途：次ページ・前ページ・最終ページへの移動、ページリセット確認

#### ステータス変更用（手順書 7.1.1 / 7.1.2）

- **Case 40**：正方向のステータス変更が可能なデータ
  - `status` = （変更前ステータス）

- **Case 41**：取消・差戻が可能なデータ
  - `status` = （取消・差戻が可能なステータス）

#### 一括操作用（手順書 8.2.1 / 8.2.2 / 8.3.1 / 8.4.1 / 8.5.1）

- **Case 50〜52**：削除可能なデータ × 3件
  - `status` = （削除可能なステータス）
  - `owner` = Case 2（自分）または削除権限のあるユーザーが所有

- **Case 53**：削除不可なデータ
  - `status` = （削除不可ステータス）

- **Case 54〜**：一括ステータス更新・一括登録用のデータ（件数は上限値に合わせて作成）
  - `status` = （一括操作可能なステータス）
