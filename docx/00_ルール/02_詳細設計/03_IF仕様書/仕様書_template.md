# IFID IF名

> [記] ファイル名：`IFID_IF名.md`（例：`IF-01-01_注文データCSV出力.md`）

## 基本情報

| 項目 | 内容 |
| :--- | :--- |
| **IFID** | |
| **IF名** | |
| **出力形式** | |
| **文字コード** | |
| **実行タイミング** | |

> [記] 出力形式の例：「CSV（カンマ区切り、ダブルクォーテーション囲みあり、ヘッダー行あり）」「Excel（xlsx）」「PDF」
> [記] 文字コードの例：`UTF-8`、`Shift_JIS`
> [記] 実行タイミングの例：「画面の○○ボタン押下時」「毎日 02:00 のバッチ処理」

---

## 目的

> [記] このIFで何を行うのかを1〜2文で記載する

---

## 対象データ

> [記] 抽出対象のデータ範囲（期間・ステータスなど）を箇条書きで記載する

---

## 機能仕様

### ■ 項目一覧

| No | 項目名 | 物理名 | 型 | 繰り返し |
| :--- | :--- | :--- | :--- | :--- |

> [記] 型：`string`、`number`、`date`、`boolean` など
> [記] 繰り返し：1始まりで、別の繰り返し配列には +1 した番号を割り振る。なければ `-`

#### ▶ 備考

| 項目名 | 備考 |
| :--- | :--- |

> [記] 特記事項がある項目のみ記載する。なければこのセクションごと削除してよい

### ■ 書式（Excel / PDF 出力時のみ）

> [記] CSV出力の場合は本セクションごと削除してよい。Excel / PDFで出力する場合のみ記載する

| 項目名 | フォント | 文字サイズ | 太字 | 文字色 | 背景色 | セル幅 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |

> [記] 太字・文字色・背景色などがない場合は `-`

#### ▶ 備考

| 項目名 | 備考 |
| :--- | :--- |

> [記] 「右寄せ」「カンマ区切り」「結合セル」など、書式に関する特記事項を記載する

### ■ 変換・抽出ロジック

| 項目名 | ロジック |
| :--- | :--- |

> [記] 例：「そのまま出力」「`YYYY/MM/DD HH:mm:ss` 形式に変換」「`orders.customer_id` で `customers` をJOINして取得」

---

## 参照テーブル

### ■ `テーブル名`

| 項目名 | カラム | データ型 |
| :--- | :--- | :--- |

> [記] 参照元テーブル・カラムを記載する。テーブル名・カラム名はバッククォートで囲む。データ型は大文字（`VARCHAR`、`BIGINT` 等）

---

## 備考

- 
- 

> [記] 0件時の挙動、最大件数の制限、ファイル名の命名規則など、全体に関わる特記事項を記載する
