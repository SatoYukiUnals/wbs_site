# 【結合テスト：データ一覧】（対象：レビュー管理機能）

---

## 1. 事前作成データ

### ■ `テナント`（Case 1〜）

- **Case 1**：テスト用テナント

### ■ `ユーザー`（Case 2〜）

- **Case 2**：adminユーザー（`role` = `admin`、レビュアー役）
- **Case 3**：memberユーザー（`role` = `member`、担当者役）

### ■ `プロジェクト`（Case 5〜）

- **Case 5**：レビューテスト用プロジェクト
  - メンバー：Case 2（admin）、Case 3（member）

### ■ `タスク`（Case 10〜）

- **Case 10**：承認テスト用タスク（レビュー待ち）
  - `title` = 承認テストタスク
  - `status` = レビュー待ち
  - 担当者：Case 3
  - `project` = Case 5
  - ※ reviewレコードも事前作成（`status` = pending）

- **Case 11**：レビュー依頼テスト用タスク（進行中）
  - `title` = レビュー依頼テストタスク
  - `status` = 進行中
  - 担当者：Case 3
  - `project` = Case 5

- **Case 12**：差し戻しテスト用タスク（レビュー待ち）
  - `title` = 差し戻しテストタスク
  - `status` = レビュー待ち
  - 担当者：Case 3
  - `project` = Case 5
  - ※ reviewレコードも事前作成（`status` = pending）

- **Case 13**：完了ロックテスト用タスク（完了・レビューapproved）
  - `title` = 完了ロックテストタスク
  - `status` = 完了
  - 担当者：Case 3
  - `project` = Case 5
  - ※ reviewレコードも事前作成（`status` = approved）
