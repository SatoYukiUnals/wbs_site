# 機能仕様書：処理ロジック詳細

## 1. ■概要・目的

処理フロー（05）の各ステップで参照される、バリデーション条件・登録内容・処理制御の詳細を定義します。What（対象データ）と How（制御ロジック）に分けて記述します。

## 2. ■事前準備

- [ ] 処理フロー（05）の記述完了

## 3. ■作業手順（Issueのタスクリストとして利用）

- [ ] **バリデーション条件の定義**（6.1）：各項目の必須・形式・桁数などの条件を記述します。処理フローの操作種別が複数ある場合は、操作ごとに小項目（`#### ▶ 8.1.x 操作名（7.x）`）で区切り、処理フロー側では `（詳細は 8.1.x 参照）` の形式で項番を使って紐づけます。
- [ ] **登録内容の定義**（6.2）：INSERT/UPDATE するカラムと登録値を記述します。
- [ ] **処理制御の定義**（6.3）：トランザクション・重複判定など制御ロジックを記述します。**判定・制御（条件分岐を伴うロジック）は箇条書きではなく表形式で記述します。**
- [ ] **処理フローとの対応確認**：各サブセクションが処理フローのどのステップに対応するかを確認します。

## 4. ■セルフチェック項目（作業完了後の最終確認）

- [ ] バリデーション条件が項目ごとに定義されている
- [ ] 登録・更新するカラムと値が明記されている
- [ ] トランザクション等の処理制御が明記されている
- [ ] 判定・制御（条件分岐を伴うロジック）が表形式で記述されている
- [ ] 処理フロー（05）の `（詳細は8.x参照）` と各サブセクションが対応している
- [ ] 操作種別が複数ある場合、バリデーション条件（8.1）が操作ごとの小項目（8.1.1, 8.1.2 …）に分割されており、処理フローから `（詳細は 8.1.x 参照）` で紐づいている

## 5. ■成果物・参考資料

- 成果物の場所：

## 6. ■サンプル

### 6.1 バリデーション条件（What）

<!-- 5. 処理フロー の「バリデーション」ステップに対応 -->

**操作種別が1つの場合：** 単一テーブルで記述する。

| No | 項目名 | 条件 | 備考 |
| :--- | :--- | :--- | :--- |
| 1 | | | |

**操作種別が複数ある場合：** 操作ごとに `#### ▶ 8.1.x` の小項目で区切り、処理フロー側は `（詳細は 8.1.x 参照）` で紐づける。

#### ▶ 8.1.1　〇〇の登録（7.1）

| No | 項目名 | 条件 | 備考 |
| :--- | :--- | :--- | :--- |
| 1 | | | |

#### ▶ 8.1.2　〇〇の変更（7.2）

| No | 項目名 | 条件 | 備考 |
| :--- | :--- | :--- | :--- |
| 1 | | | |

### 6.2 登録内容（What）

<!-- 5. 処理フロー の「DB操作」ステップに対応 -->

| No | 対象カラム | 登録内容 | 備考 |
| :--- | :--- | :--- | :--- |
| 1 | | | |

### 6.3 処理制御（How）

<!-- 5. 処理フロー の「DB操作」ステップに対応 -->

記述ルール：

- 単一の方針・宣言（トランザクション方針など条件分岐を伴わないもの）は箇条書きで記述する。
- **判定・制御（条件によって処理結果が分岐するロジック）は必ず表形式で記述する。** 箇条書きのネストで条件分岐を表現しない。

#### ▶ 方針（条件分岐なし）

- **トランザクション**：INSERT/UPDATE 失敗時は即時ロールバックし、処理前の状態を保持する。

#### ▶ 判定・制御（条件分岐あり）

| No | 判定・制御項目 | 条件 | 処理結果 | 備考 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | | | | |
