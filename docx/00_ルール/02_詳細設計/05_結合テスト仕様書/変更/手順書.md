> テスト実施前に [01_初期データ.md](01_初期データ.md) に記載のデータを作成してから各テストを実施すること。
> **【記載ルール】** テスト内容・期待結果に画面仕様書・設計書への参照は記載しない。期待結果は具体的な値・状態・HTTPステータスで記述する。テスト共通のドキュメント（データ一覧の `Case No.` 等）への参照はこの限りではない。DBを直接参照・確認しない。更新結果の確認は詳細画面・一覧画面等のGUIで行う。

---

# 1. 初期表示・画面状態

## 1.1. フォームの初期表示

### ■ 1.1.1. 既存データが各フィールドに表示されること

#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 22

#### ▶ テスト内容
1. 対象データの変更画面を表示する
2. 各フィールドの表示値を目視で確認する

#### ▶ 期待結果

→ [1.1. 初期表示_フィールド期待値.md](./1.1.%20初期表示_フィールド期待値.md) の表のとおりであること

---

### ■ 1.1.2. URLダイレクトアクセスによる初期表示

#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 22

#### ▶ テスト内容
1. 変更画面のURLに直接アクセスする（例：`/orders/203/edit/`）

#### ▶ 期待結果
- [ ] エラー画面が表示されず、Case 22 の変更フォームが正常に表示されること

---

# 2. 正常更新

## 2.1. 基本更新

### ■ 2.1.1. 内容を修正した正常更新

#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 22

#### ▶ テスト内容
[2.1. 正常更新_変更対象フィールド.md](./2.1.%20正常更新_変更対象フィールド.md) の各フィールドに対して以下を実施する。

1. 対象データの変更画面を表示する
2. 対象フィールドを変更後の値に書き換える
3. 「更新」ボタンを押下する

#### ▶ 期待結果
- [ ] 詳細画面に遷移すること
- [ ] 詳細画面で [2.1. 正常更新_変更対象フィールド.md](./2.1.%20正常更新_変更対象フィールド.md) の「詳細画面での期待表示」のとおり値が表示されること
- [ ] 更新成功のトーストが表示されること

---

# 3. ステータス遷移

## 3.1. 正常遷移

### ■ 3.1.1. 各ステータス遷移（正方向）

#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 22

#### ▶ テスト内容
1. 遷移可能なステータス（`ordered`）のデータの変更画面を表示する
2. `status` ドロップダウンを `shipping`（配送中）に変更して「更新」ボタンを押下する

#### ▶ 期待結果
- [ ] 詳細画面に遷移すること
- [ ] 詳細画面の `status` が `shipping`（配送中）と表示されること
- [ ] 更新成功のトーストが表示されること

---

## 3.2. 遷移禁則

### ■ 3.2.1. 遷移不可なステータスへの変更

#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 23

#### ▶ テスト内容
1. 遷移不可のステータス（`completed`）のデータの変更画面を表示する
2. `status` ドロップダウンを `ordered`（受注済み）に変更して「更新」ボタンを押下する

#### ▶ 期待結果
- [ ] 変更画面から遷移しないこと
- [ ] 遷移不可のエラーメッセージが表示されること

---

# 4. バリデーション・異常系

## 4.1. 排他制御

### ■ 4.1.1. 古いバージョン番号での更新

#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 22

#### ▶ テスト内容
1. 対象データの変更画面を表示する
2. 別タブ・別セッションで同じデータを更新する
3. 最初の画面から「更新」ボタンを押下する（古いバージョンのまま送信）

#### ▶ 期待結果
- [ ] 変更画面から遷移しないこと
- [ ] 排他エラーのメッセージが表示されること
- [ ] 詳細画面を開き直したとき、手順2で更新した値のみ反映されていること

---

## 4.2. 存在チェック

### ■ 4.2.1. 存在しないIDへの更新

#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：なし

#### ▶ テスト内容
1. DBに存在しないIDを含む変更画面URLに直接アクセスする（例：`/orders/99999/edit/`）

#### ▶ 期待結果
- [ ] 404 エラーページが表示されること

---

# 5. 権限制御

## 5.1. 表示制御

### ■ 5.1.1. ロールによるボタンの表示制御

→ [ロールによるボタンの表示制御.md](./ロールによるボタンの表示制御.md) を参照

---

## 5.2. URLダイレクトアクセス

### ■ 5.2.1. 権限なし・権限あり・特殊ロール

#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 22

#### ▶ テスト内容
1. 権限のないユーザーで変更画面URLに直接アクセスする
2. 権限のあるユーザーで変更画面URLに直接アクセスする
3. 特殊ロールのユーザーで変更画面URLに直接アクセスする

#### ▶ 期待結果
- 手順1：変更画面が表示されず、「ログイン画面」またはエラーページ（403）にリダイレクトされること
- 手順2：エラー画面が表示されず、変更画面が正常に表示されること
- 手順3：特殊ロールに対応した画面が表示され、そのロールに対応したボタンのみ表示されること

---

### ■ 5.2.2. 他テナントのデータへのアクセス

#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：（他テナントのデータID）

#### ▶ テスト内容
1. 他テナントのデータIDを含む変更画面URLに直接アクセスする

#### ▶ 期待結果
- [ ] 403 または 404 エラーページが表示されること
- [ ] 他テナントのデータが表示・変更できないこと

---

# 6. 非機能・その他

## 6.1. 連打対策

### ■ 6.1.1. 全ボタン・全操作でAPIが1回しか実行されないこと

#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 22

#### ▶ テスト内容
1. 変更内容を入力する
2. 「更新」ボタンをすばやく2〜3回連続して押下する

#### ▶ 期待結果
- [ ] 1回目の押下後にボタンが非活性になること
- [ ] 詳細画面を開き直したとき、変更した内容が1回分のみ反映されていること

---

## 6.2. 操作フィードバック

### ■ 6.2.1. 操作成功後のトースト表示（メッセージ内容・表示タイミング）

#### ▶ 前提条件
- 画面名：
- ログインユーザー：
- 操作対象データ：Case 22

#### ▶ テスト内容
1. 正常な変更内容を入力して「更新」ボタンを押下する

#### ▶ 期待結果

| 操作 | トーストのメッセージ | 表示タイミング | 消えるタイミング |
|------|-----------------|-------------|--------------|
| 更新 | （仕様書のメッセージを記載） | 更新完了直後 | {N}秒後に自動消去 |
